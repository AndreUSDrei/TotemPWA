@model TotemPWA.Models.Combo
@{
    ViewData["Title"] = "Detalhes do Combo";
}
<h2>Detalhes do Combo</h2>
<dl class="row">
    <dt class="col-sm-2">ID</dt>
    <dd class="col-sm-10">@Model.Id</dd>
    <dt class="col-sm-2">Produtos</dt>
    <dd class="col-sm-10">
        @if (Model.ComboProducts != null && Model.ComboProducts.Any())
        {
            <ul>
            @foreach (var cp in Model.ComboProducts)
            {
                <li>@cp.Product?.Name (@cp.Product?.Price.ToString("C"))</li>
            }
            </ul>
        }
        else
        {
            <span>Nenhum produto.</span>
        }
    </dd>
</dl>
<a href="@Url.Action("Edit", new { id = Model.Id })" class="btn btn-warning">Editar</a>
<a href="@Url.Action("Index")" class="btn btn-secondary">Voltar</a> 