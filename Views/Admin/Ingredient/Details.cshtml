@model TotemPWA.Models.Ingredient
@{
    ViewData["Title"] = "Detalhes do Ingrediente";
}
<h2>Detalhes do Ingrediente</h2>
<table class="table">
    <tr><th>Nome</th><td>@Model.Name</td></tr>
    <tr><th>Preço</th><td>@Model.Price.ToString("C")</td></tr>
    <tr><th>Limite</th><td>@Model.Limit</td></tr>
</table>
<a class="btn btn-secondary" href="/Admin/Ingredient">Voltar</a>
<style>
    .btn { padding: 7px 16px; border-radius: 6px; text-decoration: none; border: none; cursor: pointer; }
    .btn-secondary { background: #888; color: #fff; }
    .btn:hover { opacity: 0.85; }
    table { margin-top: 18px; }
    th, td { padding: 8px; }
</style> 